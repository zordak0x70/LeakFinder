# TODO.md (From most to least important)

- [ ] Profile the tool in depth and test properly using modern fuzzing tools to generate noisy .git repository (In python probably)
- [X] Allow user-defined rules and regexes compatible with re2c (VectorScan-compliant implementation)
- [ ] Add a terminal UI and improve user experience
